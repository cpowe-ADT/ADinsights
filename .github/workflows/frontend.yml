name: Frontend CI

on:
  pull_request:
    branches:
      - main
    paths:
      - 'frontend/**'
      - '.github/workflows/frontend.yml'

jobs:
  test:
    name: Test and build frontend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: |
            package-lock.json
            frontend/package-lock.json

      - name: Install root dependencies
        run: npm ci

      - name: Check formatting
        run: bash scripts/ci/prettier_changed_check.sh

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: Run unit tests with coverage
        id: test
        working-directory: frontend
        env:
          CI: 'true'
        run: npm test -- --run --coverage --reporter=default --reporter=json --outputFile=test-results/vitest-report.json

      - name: Build frontend
        working-directory: frontend
        run: npm run build

      - name: Archive frontend dist
        if: ${{ success() }}
        working-directory: frontend
        run: |
          rm -f frontend-dist.zip
          zip -r frontend-dist.zip dist

      - name: Generate frontend artifact manifest
        if: ${{ always() }}
        run: node scripts/ci/frontend-artifact-manifest.mjs

      - name: Upload frontend artifacts
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: frontend-ci-artifacts
          if-no-files-found: warn
          path: |
            frontend/frontend-dist.zip
            frontend/coverage
            frontend/frontend-ci-artifacts.json

      - name: Post coverage summary
        if: ${{ always() }}
        run: node scripts/ci/coverage-summary-helper.mjs
