selectors:
  - name: staging_ci
    description: 'Fast CI selector for staging and reference models.'
    definition:
      union:
        - method: path
          value: models/staging
        - method: path
          value: models/reference
